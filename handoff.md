# SunFood — Offer & Approval System

**Handoff** · อัปเดต 21 กันยายน 2026

ระบบเสนอราคา–อนุมัติ–ปิดการขาย ของทีม Export Sales และ Food Supply Chain
ทั้งระบบอยู่ในไฟล์เดียว: `index.html` (ไม่มี build step ไม่มี dependency นอกจาก Google Fonts)

---

## URL ใช้งานจริง

| ที่ | ลิงก์ | อัปเดตยังไง |
|---|---|---|
| GitHub Pages | https://nichasenapeng.github.io/Offer_price/ | push แล้ว build เอง |
| Cloudflare Worker | https://sunfood-offer.nichasenapeng13.workers.dev | ต้อง `wrangler deploy` เอง |

**ลิงก์เข้าโหมดตรง** — ต่อท้าย `?mode=offer` · `approve` · `status` · `summary` · `setting`
**ดูพร้อมข้อมูลตัวอย่าง** — ต่อท้าย `?demo=1` (ใช้ร่วมกับ `mode` ได้)

---

## 5 โหมดในไฟล์เดียว

หน้าแรกเป็นการ์ดสี่เหลี่ยมให้เลือกโหมด กดโลโก้ SUNFOOD ที่มุมซ้ายบนเพื่อกลับหน้านี้เสมอ

| โหมด | ใคร | ทำอะไร |
|---|---|---|
| **Offer** | ทีมขาย | กรอกใบเสนอราคา 3 ขั้น (Customer → Inquiry → สรุป & ส่ง) |
| **Approve** | ผู้อนุมัติ (ต้องใส่รหัสผ่าน) | Approve / Reject / **มีคำถาม** แยก 2 ช่อง A1, A2 |
| **Status** | ทีมขาย | ตามผลหลังอนุมัติ ปิดการขาย ตอบคำถาม ขอขยาย VALID ขอ bidding |
| **Summary** | ทุกคน | สรุปการปิดการขายรายวัน ปริมาณ (MT) และมูลค่า (USD) |
| **Setting** | แอดมิน | ค่าเริ่มต้นฟอร์ม · **ตารางค่าเฟรทรายเดือนแยก port** · ลิงก์ระบบ |

---

## Flow สถานะ

```
ส่งใบ → pending
          ├─ A2 กด Approve               → approved ทันที (A1 ถูกเติมให้อัตโนมัติ)
          ├─ A1 กด Approve               → partial → (รอ A2 กด) → approved
          ├─ ใครกด Reject                → rejected → ทีมขายกด "แก้ไขและส่งใหม่" → ใบใหม่ REF ใหม่
          │                                            หรือกด "ลบใบนี้" → deleted (ซ่อน กู้คืนได้)
          └─ ใครกด "มีคำถาม"             → question → ทีมขายตอบในใบเดิม → กลับมาให้เคาะ

approved → ทีมขายเลือก
          ├─ ปิดการขายได้     → closed-won   (บันทึก shipment/qty/ราคา/margin/remark/วันที่)
          ├─ ไม่ปิดการขาย     → closed-lost  (เสนอไปแล้วแต่ไม่ได้)
          ├─ ไม่เสนอ          → not-offered  (ไม่ได้ส่งราคาให้ลูกค้าเลย)
          ├─ ให้ไอเดียราคา    → idea-given   (ยังไม่รู้ผล รอติดตาม — กดปิด/ไม่ปิดการขายต่อได้)
          ├─ ขอขยาย VALID     → ext-pending → ผู้อนุมัติยืนยัน/ไม่อนุมัติ (REF เดิม ไม่ออกใบใหม่)
          │                     ระหว่างรอ ทีมขายกด "แก้ไขคำขอ" หรือ "ยกเลิกคำขอ" ได้
          └─ ลูกค้าขอ bidding → ใบเดิมเป็น superseded + เปิดฟอร์มใหม่พร้อมข้อมูลเดิม
```

**Approve 2 กดอนุมัติ = จบใบเลย** (21 ก.ย. 2026) — `vote()` เติม `A1 = approved` ให้อัตโนมัติ
ไม่ต้องรอ Approve 1 กด ใบขึ้นเป็น `approved` ทันที

- เติมให้**เฉพาะตอน A1 ยังไม่ได้กดอะไร** ถ้า A1 กดไปแล้วจะไม่ไปทับของเขา
- **A1 ปฏิเสธไว้ก่อน แล้ว A2 กดอนุมัติ → ใบยังเป็น `rejected`** เพราะ `getStatus()` ให้ reject ชนะเสมอ
- A1 พิมพ์ความเห็นไว้แต่ยังไม่ได้กดปุ่ม → ถือว่ายังไม่ได้กด ระบบเติมให้เหมือนกัน ความเห็นเดิมไม่ถูกลบ
- ถ้ามีคำถามค้างอยู่ การกดของ A2 ล้างคำถามทิ้งเหมือนการโหวตปกติ
- ชีทเก็บได้แค่คำว่า `approved` เหมือนกดเอง แยกไม่ออกว่าใครกด จึงเขียน
  `อนุมัติอัตโนมัติพร้อม Approve 2` ลงหมายเหตุ A1 ให้ (เฉพาะตอนช่องนั้นยังว่าง)
- **ทิศทางเดียว** — A1 กดคนเดียวยังเป็น `partial` รอ A2 เหมือนเดิม
- ราคาที่อนุมัติยังยึดของ A2 ตามเดิม (`approvedPrice_()`)

**ระหว่างมีคำถามค้าง ปิดการขายไม่ได้** — ตั้งใจให้เป็นแบบนั้น

**ใบที่เลือก "ให้ไอเดียราคา" ขอขยาย VALID ไม่ได้** — เพราะไม่ได้ยืนราคาไว้ตั้งแต่ต้น

**คำขอขยาย VALID แก้ได้จนกว่าจะถูกเคาะ** — ระหว่าง `ext-pending` ทีมขายกด "แก้ไขคำขอ"
เพื่อเปิดฟอร์มเดิมที่กรอกค่าไว้แล้ว หรือ "ยกเลิกคำขอ" เพื่อกลับไปเป็น `approved`
ช่องเหตุผลเป็น textarea ขึ้นบรรทัดได้ และผู้อนุมัติมีช่อง "หมายเหตุถึงทีมขาย" ตอนเคาะ
เก็บเป็นช่องที่ 4 ของ `ext-*` (`extNote`) แถวเก่าที่มีแค่ 3 ช่องอ่านได้เหมือนเดิม

**"ลบ" คือซ่อน ไม่ใช่ลบจริง** — ใบที่ถูกปฏิเสธกดลบได้ (มี confirm) จะกลายเป็นสถานะ
`deleted` ซึ่ง `getStatus()` คืนก่อนทุกสถานะ ทำให้หายจากทุกรายการและไม่ถูกนับใน "ทั้งหมด"
แต่**แถวในชีทยังอยู่ครบ** ดูและกู้คืนได้จากปุ่มกรอง "ถังขยะ" ทั้งฝั่งผู้อนุมัติและเช็คสถานะ

ลบได้เฉพาะสถานะ `rejected` — `deleteOffer()` เช็คซ้ำก่อนทำงาน ต่อให้เรียกจากที่อื่นก็ไม่ผ่าน
ถ้าอยากลบแถวออกจากชีทจริง ต้องแก้ Apps Script ฝั่ง server ซึ่งตอนนี้เขียนได้แค่คอลัมน์สถานะ

**`idea-given` คือดีลที่ยังเปิดอยู่** (เพิ่ม 17 ก.ย. 2026) — ทีมขายให้ไอเดียราคากับลูกค้าแล้ว
แต่ยังไม่รู้ผล เก็บใน `approvals[ref].close` เหมือนสถานะปิดการขาย เพื่อใช้ช่องเก็บเดิม
แต่**ไม่ได้ปิด**: การ์ดยังมีปุ่มปิดการขายได้ / ไม่ปิด / ไม่เสนอ ครบ และมีกล่องเหลืองบอกว่าให้อะไรไปเมื่อไร
- บังคับกรอก Remark ว่าให้ไอเดียอะไรไป ไม่งั้นตอนตามต่อจะไม่รู้ว่าคุยค้างไว้ที่ไหน
- กด "อัปเดตไอเดียราคา" ซ้ำได้ Remark เดิมถูกเติมไว้ในช่องให้แก้ต่อ
- ช่องวันที่เริ่มที่วันนี้ ไม่ใช่วันที่ให้ไอเดีย เพราะกดต่อมักเป็นการปิดการขาย
- นับเป็นงานที่ต้องทำในหน้าเช็คสถานะ (ป้ายตัวเลขบนปุ่มกรอง + ป้ายแจ้งเตือนรวม)
- ซ่อนปุ่มกรองนี้ในหน้า Approve เหมือนสถานะปิดการขายอื่น
- **สรุปยอดขายไม่นับ** (`renderSummary_` นับเฉพาะ `closed-won`)
- ปุ่ม "ขอขยาย VALID" / "ลูกค้าขอ bidding" ขึ้นเฉพาะใบ `approved` — ใบที่ให้ไอเดียไปแล้วต้องกด "ยกเลิก" ก่อน
- `saveApprovedPrice()` แก้ราคาได้ในสถานะนี้ และส่ง remark/วันที่เดิมกลับไปครบ
  (ถ้าส่ง `idea-given` เปล่า ๆ remark จะหายจากชีท)
- **`vote()` ไม่ได้แก้** — ส่ง `status` เปล่า ๆ อยู่ ถ้าผู้อนุมัติกดยกเลิกแล้วโหวตใหม่บนใบที่ให้ไอเดียไปแล้ว
  remark จะหาย ไม่ได้แก้เพราะ Apps Script ใช้ `status` ของ vote ตัดสินการแจ้ง LINE และโค้ดฝั่งนั้นไม่อยู่ใน repo

**`closed-lost` กับ `not-offered` ไม่เหมือนกัน** — อันแรกคือเสนอไปแล้วลูกค้าไม่เอา
อันหลังคือไม่ได้เสนอเลย แยกไว้เพื่อไม่ให้ตัวเลข "แพ้ดีล" เพี้ยน
ทั้งคู่เก็บแค่ remark + วันที่ ไม่มีตัวเลขปิดการขาย และกด "แก้ไข" กลับเป็น `approved` ได้
สรุปยอดขาย (`renderSummary_`) นับเฉพาะ `closed-won` ทั้งสองสถานะนี้จึงไม่กระทบยอด

---

## Backend: Google Sheets + Apps Script 2 ตัว

**Script 1 — เขียนใบเสนอราคา** (ผูกกับ Sheet "EXPORT SALES _ OFFER", tab `Inquiry Requests`)
`https://script.google.com/macros/s/AKfycbyAfBG-BNJfw7y7MnbC6t4zC4Vckewu-VGQBBgSnEhAXqHEYRyFxjmKuvoLZxpM-Lw/exec`
รับ GET `?data=<JSON>` → เขียน 1 แถวต่อ 1 สินค้า
สำเนาโค้ดอยู่ใน `Code.gs` แต่ **ตัวที่ deploy จริงใหม่กว่า อย่า copy ทับทั้งไฟล์**

**Script 2 — อ่านทั้งหมด + เขียนผลอนุมัติ** (ผูกกับ Sheet "EXPORT SALES _ APPROVE")
`https://script.google.com/macros/s/AKfycbzaa16CiQbLUhCNTiIkVSuFhCfEgrYsz342t6GmOc1c-IY3KiOEmvS4ZWXcZHwMAtTS/exec`
- GET → คืนใบทั้งหมด จัดกลุ่มตาม REF พร้อม `products[]`
- POST (no-cors) → บันทึก A1/A2/หมายเหตุ/สถานะรวม

Sheet: [Offer](https://docs.google.com/spreadsheets/d/15fxIAqk32f898h4nN-CG1HstFe3GPSy-33ALiJlJHwU/edit) · [Approval](https://docs.google.com/spreadsheets/d/1ZKrNVK1Mgffl1UqTGw8QxMld3hpTnlfZgpcgrwoNyHU/edit)

---

## จุดที่ต้องรู้ก่อนแก้โค้ด

**1. ข้อมูลบางอย่างฝากไว้ในคอลัมน์ที่มีอยู่ เพราะเพิ่มคอลัมน์ในชีทไม่ได้**

คอลัมน์ `หมายเหตุ` เก็บ meta ไว้ต้นข้อความ แล้วแกะออกตอนอ่านด้วย `stripMeta_()`
```
[KIND: idea | DATE: 10/09/2026 | VALID: 7 วัน | FRT: 3000 | MTC: 24 | FX: 32 | BID: SFD-2026-7869] หมายเหตุที่ทีมขายพิมพ์
```
`BID` (เพิ่ม 17 ก.ย. 2026) = REF ใบเดิมของใบ bidding ดูหัวข้อ 7.1
`KIND` จะโผล่เฉพาะใบที่เลือก "ให้ไอเดียราคา" — ใบเก่าและใบเสนอราคาปกติไม่มีคีย์นี้ อ่านได้เหมือนเดิม

คอลัมน์ `หมายเหตุ A1` / `หมายเหตุ A2` เก็บราคาที่ผู้อนุมัติเคาะไว้ต้นข้อความ แกะด้วย `stripApMeta_()`
```
[PRICE: 4050] ความเห็นที่ผู้อนุมัติพิมพ์
[PRICE: 2650-2700 ;; 4000-4050] ใบหลายสินค้า — คั่นด้วย ;; เรียงตามลำดับสินค้าในใบ
```
ไม่มี `[PRICE: ...]` = อนุมัติตามราคาที่ทีมขายเสนอมา — แถวเก่าอ่านได้เหมือนเดิม

คอลัมน์ `สถานะรวม` เก็บสถานะแบบมี payload คั่นด้วย `|`
```
closed-won|shipment|qty|price|margin|remark|YYYY-MM-DD
closed-lost|||||remark|YYYY-MM-DD
not-offered|||||remark|YYYY-MM-DD
idea-given|||||remark|YYYY-MM-DD
ext-pending|VALID ใหม่|เหตุผล
ext-approved|VALID ใหม่|เหตุผล|หมายเหตุจากผู้อนุมัติ      ext-rejected|เหมือนกัน
question|A1                         question|A1|คำตอบจากทีมขาย
superseded|เหตุผล
deleted|YYYY-MM-DD
```
ค่าที่ผู้ใช้พิมพ์จะถูกล้าง `|` ออกก่อนเสมอ เพื่อไม่ให้ข้อมูลเพี้ยน

**2. ส่งใบเสนอราคาแบบ POST แล้ว — ไม่มีเพดาน 12,000 ตัวอักษรอีก** (24 ก.ย. 2026)
`submitWithRef_()` ยิง `fetch(APPS_SCRIPT_URL, {method:'POST', body: JSON.stringify(payload)})`
`doPost` ของ Script 1 มีอยู่แล้วตั้งแต่ก่อนหน้านี้ (ใน `Code.gs` ก็มี) จึง**ไม่ต้องแก้หรือ deploy Apps Script**
- body เป็น JSON ตรง ๆ ไม่ต้อง encode 2 ชั้นแบบตอนใช้ GET เพราะ `handle()` ลอง `JSON.parse(raw)` ก่อนเสมอ
- `Content-Type` ต้องเป็น `text/plain` — ถ้าเป็น `application/json` เบราว์เซอร์จะยิง preflight
  ซึ่ง Apps Script ไม่ตอบ แล้วจะ error ว่า `Failed to fetch`
- POST รับได้หลายสิบ MB ตัดทั้ง `URL_LIMIT` และคำเตือน "ข้อมูลยาวเกิน" ออกแล้ว
- `doGet` ยังอยู่ ใครเปิดเว็บเวอร์ชันเก่าค้างไว้ยังส่งได้ตามปกติ (แต่ยังชนเพดานเดิม)

**ประวัติ: เพดานตอนยังใช้ GET** — ส่งผ่าน `?data=` ต้อง `encodeURIComponent` 2 ชั้น
(Apps Script decode ซ้ำอีกรอบ ค่าที่มี `%` เช่น "48.6%" จะทำให้พังทั้งแถว) และ Google ปัดทิ้งด้วย 400
เมื่อ URL ยาวเกินราว 12,000–13,000 ตัว ข้อความไทยพองตัวราว 10 เท่า ใบ 3 สินค้าที่หมายเหตุยาวจึงชนง่าย
หน้า error ไม่มี CORS เบราว์เซอร์เลยขึ้นแค่ `Failed to fetch` ช่วงนั้นเลยตัด `sessions` ออกจาก payload
(ซ้ำข้อมูลทุกสินค้าและไม่มีใครอ่าน) ซึ่งยังตัดอยู่

**3. ใช้ `fetch` ไม่ใช่ `Image` beacon**
หน้า "ส่งสำเร็จ" ขึ้นเฉพาะเมื่อ server ตอบ ok จริง ถ้าล้มเหลวจะเตือนให้ส่งใหม่

**4. หมายเหตุรวมต้องติดไปทุกสินค้า**
`submitForm()` เขียน `overallRemark` ลงทุก session — เพราะ Script 2 อ่านค่าจากแถวแรกของ REF
ถ้าติดเฉพาะสินค้าชิ้นสุดท้าย ผู้อนุมัติจะไม่เห็นหมายเหตุเลย

**4.5 %Margin ในชีทมีหลายรูปแบบปนกัน** — ช่องกรอกเป็น free text และ Google Sheets
อ่าน `"25.33%"` ที่พิมพ์มาเป็นตัวเลข `0.2533` แล้วคืนค่านั้นกลับมา การ์ดเลยเคยขึ้น `0.2533%`

ตอนนี้กันไว้สองชั้น
- ตอนส่ง `pctPlain_()` ตัด `%` ออกถ้าค่าเป็นตัวเลขล้วน ชีทจะเก็บ `25.33` ตรง ๆ
- ตอนแสดง `fmtMargin_()` / `mgNum_()` แปลงค่าที่ไม่มี `%` และต่ำกว่า 1 เป็นเปอร์เซ็นต์ (×100)
  ข้อความอิสระอย่าง `11% (-3% เทียบ BLK)` หรือ `x` แสดงตามที่พิมพ์ ไม่เติม `%` ต่อท้าย

ถ้ามีวันที่ margin ต่ำกว่า 1% จริง ให้พิมพ์ `0.5%` (มี `%`) จะไม่ถูกคูณ 100
แถวเก่าในชีทไม่ได้ถูกแก้ ใช้การแปลงตอนแสดงผลเอา

**4.6 ราคาที่อนุมัติอาจไม่เท่าราคาที่เสนอ** — ผู้อนุมัติกรอก "ราคาที่อนุมัติ (USD/MT)"
ในช่องของตัวเองตอนกด Approve เว้นว่าง = อนุมัติตามราคาที่เสนอมา

`approvedPrice_()` ยึด **A2 ก่อน** ถ้า A2 ไม่ได้กรอกจึงใช้ของ A1 ไม่มีใครกรอกเลยก็ใช้ `offerPrice`
ถ้าสองคนกรอกไม่ตรงกัน ใบยังเป็น approved ตามปกติ แต่กล่องสรุปจะเปลี่ยนเป็นสีเหลือง
แสดงราคาของทั้งคู่และบอกว่าระบบยึดของ A2

ราคานี้ไปโผล่ 3 ที่: หัวการ์ดเปลี่ยนเป็น "ราคาที่อนุมัติ" พร้อมบรรทัด "เสนอไว้ ..." ข้างล่าง,
กล่องสรุปทั้งหน้าผู้อนุมัติและหน้าเช็คสถานะ, และ placeholder ของช่อง "ราคาปิดการขาย"
กด "ยกเลิก" โหวต = ราคาที่เคาะไว้ของคนนั้นหายไปด้วย

ช่องราคา**แก้ได้หลังกด Approve แล้ว** ไม่ต้องยกเลิกโหวตก่อน — กด "บันทึกราคา" หรือ Enter
`saveApprovedPrice()` กันไว้ 3 ชั้น: ช่องที่ยังไม่ได้ Approve บันทึกไม่ได้, ใบที่ปิดการขายไปแล้ว
แก้ไม่ได้, และถ้าใบนั้นมีคำขอขยาย VALID ค้างอยู่จะส่ง `ext-*` เดิมกลับไป ไม่เขียนทับ `สถานะรวม`

**ทุกใบที่อนุมัติแล้วต้องบอกราคาที่อนุมัติเสมอ** — `apPriceBox_()` ขึ้น 4 แบบ
1. บันทึกราคาไว้และต่างจากที่เสนอ → เขียว "ราคาที่อนุมัติ: $4050/MT (ทีมขายเสนอมา 4025)"
2. บันทึกไว้และเท่ากับที่เสนอ → เขียว ต่อท้ายว่า "(เท่ากับที่เสนอ)"
3. ไม่ได้บันทึกและหมายเหตุไม่มีตัวเลขน่าสงสัย → เขียว "อนุมัติตามราคาที่เสนอ: $4100/MT"
   (กรอกว่าง = อนุมัติตามราคาที่เสนอ จึงพูดออกมาให้ชัด ไม่ปล่อยให้เดาเอง)
4. ไม่ได้บันทึกแต่หมายเหตุมีราคาอื่น → เหลือง เตือนว่ายังยึดราคาเดิมอยู่

**ใบเก่าที่พิมพ์ราคาไว้ในหมายเหตุ** — `priceFromRemark_()` ดึงตัวเลขที่ดูเหมือนราคา
(**500**–999999 เอาตัวสุดท้ายที่เจอ — ขั้นต่ำ 500 เพราะ "เขย่งไอเดีย 100 USD/MT" กับ "12 MT"
เคยถูกจับมาเป็นราคา) มา**เสนอ**ให้กดยืนยัน ไม่เอาไปใช้เอง เพราะเป็นการเดาจากร้อยแก้ว
ฝั่งผู้อนุมัติจะมีปุ่ม "ใช้ 4050 จากหมายเหตุ" ฝั่งเช็คสถานะขึ้นกล่องเหลืองเตือนว่ายังไม่ได้บันทึก
และบอกให้ไปกดที่หน้า Approve ก่อนปิดการขาย — กล่องนี้หายทันทีที่บันทึกราคาแล้ว

**4.6.1 ใบหลายสินค้าเคาะราคาแยกรายสินค้าได้** (21 ก.ย. 2026) — เดิมมีช่องราคาช่องเดียวต่อ 1 ใบ
ผู้อนุมัติจึงต้องพิมพ์รวมกันมาในช่องเดียว เช่น `BB 2650-2700, SBL 4000-4050` แล้วแถวสรุป
บนการ์ดก็โชว์เป็นก้อนเดียวแบบนั้น ส่วน %Margin / Qty / ราคาเสนอ บนแถวสรุปก็เป็นของ
**สินค้าชิ้นแรกเท่านั้น** แต่ดูเหมือนเป็นของทั้งใบ (เคสจริง: `SFD-2026-9736`)

เก็บในช่องเดิม ไม่ได้แก้ Apps Script — `[PRICE: a ;; b]` เรียงตามลำดับสินค้า
- ค่าที่**ไม่มี** `;;` = ราคาเดียวของทั้งใบแบบเดิม ใบเก่าทุกใบอ่านได้เหมือนเดิม ไม่ถูกแปลงย้อนหลัง
- ใบสินค้าเดียวยังเก็บเป็นค่าเดี่ยว ไม่มี `;;` โผล่ในชีท
- หน้า Approve: ใบหลายสินค้าจะมีช่องกรอกช่องละสินค้า พร้อมชื่อสินค้ากำกับ
  (`ap-<SLOT>-<REF>-<index>`) ใบสินค้าเดียวยังใช้ `ap-<SLOT>-<REF>` ช่องเดียวเหมือนเดิม
- เว้นช่องไหนว่าง = อนุมัติตามราคาที่เสนอของชิ้นนั้น
- `readApPriceInput_()` เป็นตัวอ่านช่องกรอกตัวเดียวที่ `vote()` กับ `saveApprovedPrice()` ใช้ร่วมกัน
- แสดงผล: ตารางสินค้าเพิ่มคอลัมน์ "ราคาที่อนุมัติ" (ขึ้นเฉพาะใบที่เคาะแยกไว้)
  แถวสรุปด้านบนเปลี่ยนเป็น "แยกตามสินค้า — ดูในตาราง" แทนที่จะยกก้อนเดียวมาอ้างเป็นของทั้งใบ
  และกล่องสรุปไล่ราคาทีละสินค้า
- `priceChanged_()` คืน false เสมอเมื่อเคาะแยกรายสินค้า เพราะราคาระดับใบไม่มีความหมายแล้ว
  ช่อง "ราคาปิดการขาย" จึงกลับไปใช้ราคาที่เสนอเป็น placeholder
- ปุ่ม "ใช้ราคาจากหมายเหตุ" ขึ้นเฉพาะใบสินค้าเดียว (มันเดาได้ทีละตัวเลข)

**4.6.1.1 สินค้าตัวเดียวกันหลายแถว = ช่องราคาช่องเดียว** (24 ก.ย. 2026) — `apInputCount_(d)`
ถ้าทุกแถวมีชื่อสินค้าและ Code ตรงกัน (เป็นสินค้าเดียวที่แยกแถวตามรอบ shipment) หน้า Approve จะมีช่องราคาช่องเดียว
ป้ายเขียนว่า "ใช้กับทุกแถว" placeholder เป็นราคาเสนอของแถวแรก และเก็บเป็นค่าเดี่ยว ไม่มี `;;`
ถ้าสินค้าต่างกันยังแยกช่องเหมือนเดิม ส่วนใบเก่าที่เก็บไว้แบบ `3650 ;; 3650` (ทุกค่าเท่ากัน) จะโชว์เป็นค่าเดียวในช่อง
ที่มา: bidding สายเดียวกัน `8234 → 6220 → 2403` (NET, SKINLESS BONELESS BREAST ×2 แถว) ผู้อนุมัติต้องพิมพ์ 3650 ซ้ำสองช่อง
และใบ 2403 ทีมขายแก้ราคาแค่แถวแรกเป็น 3500 แถวที่สองยังเป็น 3700 ที่ติดมาจากใบเดิม จึงมีช่องให้กรอกราคาเก่าโผล่มาด้วย
**ยังเหลือ** — แถวราคาเก่าแบบนี้ยังโชว์ในตาราง และทำให้ %Margin / Valid บนแถวสรุปขึ้น "แยกตามสินค้า"
ต้องให้ทีมขายแก้ราคาให้ครบทุกแถวตอนทำ bidding

**4.6.2 %Margin กับ Qty/Month บนแถวสรุปก็แยกแล้ว** (21 ก.ย. 2026) — `prodVaries_(d, key)`
เช็คว่าค่ารายสินค้าต่างกันไหม ถ้าต่าง แถวสรุปขึ้น "แยกตามสินค้า — ดูในตารางด้านล่าง"
(`detailSplitCell_()`) แทนที่จะยกค่าของชิ้นแรกมาอ้างเป็นของทั้งใบ
ถ้าทุกชิ้นเท่ากันหรือมีสินค้าเดียว ยังแสดงค่าเดิมตามปกติ

ตารางสินค้าเพิ่มคอลัมน์ **%Margin (Offer)** ขึ้นเมื่อมีสินค้าชิ้นใดชิ้นหนึ่งมีค่า
(`p.offerMargin` มาจาก `%Margin (Offer)` ที่ Apps Script ส่งมาตั้งแต่ Version 11)

ช่องราคาบนแถวสรุปใช้ `fmtPrice_()` แล้ว — เดิมต่อ `$` กับ `/MT` เองทุกกรณี
ข้อความอย่าง `BB 2650-2700, SBL 4000-4050` เลยกลายเป็น `$BB 2650-2700, SBL 4000-4050/MT`

**4.6.3 Shipment ที่เสนอ / DATE / VALID แยกรายสินค้าแล้ว — ฝั่งเว็บ** (23 ก.ย. 2026)
ใช้ `prodVaries_()` + `detailSplitCell_()` ชุดเดียวกับหัวข้อ 4.6.2 ถ้าค่าของแต่ละสินค้าไม่ตรงกัน
ตารางสินค้าจะมีคอลัมน์ `Shipment ที่เสนอ` / `Date ที่จะเสนอ` / `Valid` เพิ่มเข้ามา
(เพิ่มเฉพาะช่องที่ค่าต่างกันจริง ถ้าเพิ่มคอลัมน์ Shipment ที่เสนอ หัวคอลัมน์เดิมจะเปลี่ยนเป็น `Shipment Request`)
ใบไอเดียราคาไม่แสดง DATE/VALID เหมือนเดิม ส่วน `reviseOffer()` (แก้ใบ/bidding) เปลี่ยนมาเติม
ช่อง Shipment ที่เสนอ, ราคาเสนอ, %Margin, DATE และ VALID จากค่าของสินค้าชิ้นนั้นก่อน
ถ้าชิ้นนั้นไม่มีค่าจึงใช้ค่าของทั้งใบแทน (เดิมเติมราคาของชิ้นแรกให้ทุกชิ้น)

**Script 2 แก้แล้ว — Version 17** (23 ก.ย. 2026) — เพิ่ม `Shipment ที่จะเสนอ` และ `หมายเหตุ` ต่อท้าย
`products.push()` ใน `doGet` (DATE/VALID เก็บเป็น meta อยู่ในหมายเหตุของแต่ละแถว) ใช้ deployment ID เดิม
ฝั่งเว็บอ่านด้วย `p['Shipment ที่จะเสนอ']` และ `stripMeta_(p['หมายเหตุ'])` ถ้าช่องนี้ไม่มีมา
ค่าที่อ่านได้จะว่าง และแถวสรุปแสดงค่าของทั้งใบเหมือนเดิม
ตอนแก้มีใบจริง 4 ใบที่ค่าต่างกันรายสินค้า (5800, 8509, 8441, 1514) — 8509 มี DATE ที่พิมพ์ผิดเป็น
`11/09/02026` ในแถวที่สอง จึงขึ้นเป็น "แยกตามสินค้า" ด้วย (เป็นข้อมูลในชีท ไม่ใช่บั๊ก)

**%Margin ไม่ได้คิดใหม่ตามราคาที่อนุมัติ** เพราะระบบไม่มีข้อมูลต้นทุน ยังโชว์ค่าที่ทีมขายกรอกมา

**4.7 ช่องบังคับกรอก** — เคยมีใบส่งมาโดยกรอกแค่ชื่อสินค้ากับ Code ทำให้ผู้อนุมัติ
ต้องเคาะราคาโดยไม่มี Idea Price, %Margin, Qty, VALID เลย และระบบซ่อนช่องว่างเงียบ ๆ
จนการ์ดดูเหมือนกรอกครบ

`REQ_PROD` บังคับทุกการ์ด (ประเภทสินค้า, Quantity Request, Idea Price)
`REQ_OFFER_ALL` บังคับเฉพาะ**รายการแรกของชุดแรก** (Shipment, ราคาเสนอ, %Margin,
Qty/Month, DATE, VALID) เพราะทีมขายกรอกช่องระดับใบครั้งเดียว และฝั่งอ่านก็หยิบจากแถวแรกของ REF
ดาว `*` ของช่องกลุ่มหลังจะซ่อนอัตโนมัติเมื่อมีชุดที่บันทึกแล้ว (คลาส `has-saved`)

โหมดไอเดียราคาไม่บังคับ Shipment / DATE / VALID เพราะไม่มีการยืนราคา

**ถ้าไม่มีข้อมูลจริง ๆ ให้ใส่ "ไม่มี"** — มีปุ่ม «เติม "ไม่มี" ในช่องที่ว่าง» ท้ายการ์ด
เติมเฉพาะช่องบังคับที่ยังว่างของการ์ดนั้น ตั้งใจให้ "ไม่มี" ต่างจาก "ลืมกรอก" อย่างชัดเจน
ตรวจ 3 จุด: `goNext(2)`, `addMoreProduct()` และ `submitForm()`

**4.8 DATE / VALID เป็นปฏิทิน** — ใช้ `<input type="date">` แล้ว
ชีท**ยังเก็บเป็น `DD/MM/YYYY` เหมือนเดิม** (ข้อมูลจริงทุกแถวเป็นรูปแบบนี้) แปลงเข้า-ออกด้วย
`toISO_()` / `fromISO_()` ที่ขอบเท่านั้น ค่าเก่าที่ไม่ใช่วันที่ เช่น `x` หรือ `7 วัน`
จะกลายเป็นช่องว่างตอนเปิดขึ้นมาแก้ ไม่ทำให้พัง

DATE เติมวันนี้ให้อัตโนมัติตอนสร้างการ์ด (ข้อมูลจริงคือวันที่ส่งใบเสมอ)
ปุ่มเติม "ไม่มี" ข้ามช่องปฏิทิน เพราะใส่ข้อความลงไปไม่ได้ — ทั้งสองช่องยังบังคับกรอกอยู่
แต่ DATE มีค่าให้แล้ว เหลือ VALID ที่กดเลือกครั้งเดียว (โหมดไอเดียราคาไม่บังคับทั้งคู่)

**4.9.1 Shipment เป็นช่วงเดือน ไม่ใช่วันที่** — `fmtShipment_()` แปลง Date ที่เป็น
**วันที่ 1 เที่ยงคืน** (ร่องรอยว่าพิมพ์มาแค่ "OCT 2026") กลับเป็น `OCT 2026`
ถ้าเป็นวันอื่นแปลว่าตั้งใจระบุวัน จะได้ `15 NOV 2026`
ใช้กับ `Shipment ที่จะเสนอ` และ `Shipment Request` เท่านั้น — ช่องอื่นยังใช้ `cleanCell_` (DD/MM/YYYY)
ข้อความอย่าง `OCT/NOV 2026`, `DEC`, `x` ไม่ถูกแตะ

**4.9 Google Sheets แปลงข้อความเป็นวันที่เอง** — พิมพ์ "Oct 2026" ลงช่อง Shipment
ชีทเก็บเป็น Date แล้ว Apps Script คืนมาเป็น `Thu Oct 01 2026 00:00:00 GMT+0700 (เวลาอินโดจีน)`
`cleanCell_()` ดักรูปแบบนี้ (และ ISO) แล้วแปลงเป็น `DD/MM/YYYY` ใช้กับทุกช่องที่อ่านจากชีท
รากเดียวกับเคส %Margin ที่ `"25.33%"` กลายเป็น `0.2533`

**4.10 ตารางสินค้าแสดงเฉพาะค่าของสินค้าชิ้นนั้น** — เดิม `%Margin (Idea)` ยืมค่า
`d.offerMargin` ระดับใบมาใส่ทุกแถวเมื่อสินค้าไม่มีค่าของตัวเอง ทำให้ดูเหมือนทุกชิ้น
margin เท่ากัน ทั้งที่เป็นตัวเลขของทั้งใบตัวเดียว ตอนนี้ไม่มีค่าก็ขึ้น `—` ตรง ๆ

เพิ่มคอลัมน์ **ราคาเสนอ** รายสินค้า อ่านจาก `p['ราคาเสนอ (USD/MT)']`
**Apps Script ที่ deploy อยู่ตอนนี้: Version 17** (23 ก.ย. 2026)
ทุกครั้งที่แก้ต้อง deploy ใหม่เสมอ — บันทึกโค้ดอย่างเดียวไม่มีผลกับ web app

**แก้ Apps Script แล้วเมื่อ 14 ก.ย. 2026** — เพิ่ม `ราคาเสนอ (USD/MT)` กับ `%Margin (Offer)`
ลงใน `products.push()` ของ `doGet` (Version 11, deployment ID เดิม `AKfycbzaa16Ci...`)
เดิมส่งมาแค่ 6 ช่องต่อสินค้า ราคาเสนอถูกอ่านจากแถวแรกของ REF แถวเดียวแล้วใช้เป็นราคาทั้งใบ
ราคาของสินค้าชิ้นอื่นจึงถูกทิ้งตั้งแต่ฝั่ง server ทั้งที่ทีมขายกรอกมาครบ

Script 2 อยู่ที่โปรเจกต์ `18PRGRxI9Ddfnt5fnKxkX9NWcoralAbmnPbCiZVmqkaYWhqkGWL7QvSRc`
เปิดผ่านชีท Approval → Extensions → Apps Script (ในรายการ My Projects ชื่อ "Untitled project" ซ้ำกันหลายตัว)
เวลาแก้ ต้อง **Deploy → จัดการการทำให้ใช้งานได้ → แก้ตัวเดิม → New version** เท่านั้น
ถ้ากด "การทำให้ใช้งานได้ใหม่" จะได้ URL ใหม่แล้วเว็บชี้ผิดที่

**4.10.1 หมายเหตุของผู้อนุมัติต้องเห็นในหน้าเช็คสถานะ** — เดิม `remarkA1/A2` ถูกแสดง
เฉพาะตอนใบถูก **rejected** กับตอนมี **question** เท่านั้น ใบที่อนุมัติผ่านไม่แสดงเลย
ทั้งที่มักเป็นเงื่อนไขที่ทีมขายต้องรู้ เช่น "ยืนได้ถึงสิ้นเดือนเท่านั้น"
`approverNotes_()` แสดงทั้ง A1 และ A2 ใต้กล่องราคาที่อนุมัติ (เขียว = อนุมัติ, แดง = ปฏิเสธ)
ช่องว่างไม่ขึ้นกล่องเปล่า และ `[PRICE: ...]` ถูกแกะออกตั้งแต่ตอนอ่านจากชีทแล้ว

**4.10.2 หน้าเช็คสถานะบอกว่าผู้อนุมัติแต่ละคนกดอะไร** (23 ก.ย. 2026) — หน้านี้ไม่มีช่องโหวต
และกล่องหมายเหตุจะขึ้นเฉพาะตอนมีข้อความ ใบที่อนุมัติโดยไม่พิมพ์อะไรไว้จึงดูเหมือนไม่มีของ A2 เลย
(ทีมขายถามเข้ามาจากใบ `SFD-2026-3737`) ตอนนี้ `apVoteLine_()` แสดงบรรทัด
`Approve 1 ✓ อนุมัติ · Approve 2 ✓ อนุมัติ` เหนือกล่องราคาที่อนุมัติ ในการ์ดที่อนุมัติแล้ว / ให้ไอเดียราคา / ปิดการขายแล้ว
ใบ pending / partial ไม่แสดงในหน้าเช็คสถานะอยู่แล้ว (ไม่มีปุ่มกรอง) จึงไม่ได้เพิ่มให้

**4.11 แจ้งเตือน LINE เมื่อผู้อนุมัติกด "มีคำถาม"** — อยู่ใน Apps Script ทั้งหมด
**ไม่ต้องแก้ `index.html` เลย** เพราะ `askQuestion()` ยิง `question|A1` ผ่าน `postSaleState()` อยู่แล้ว
`doPost` เรียก `notifyQuestion_(payload)` ก่อน return ซึ่งจะส่งเมื่อ `status` เป็น `question|SLOT`
เท่านั้น (`question|SLOT|คำตอบ` = ทีมขายตอบแล้ว ไม่ส่งซ้ำ)

ตั้งค่าที่ Project Settings > Script Properties — **token ไม่อยู่ในโค้ดและไม่อยู่ใน git**
- `LINE_TOKEN` = Channel access token ของ LINE OA "Export Team" (@382ibskr)
- `LINE_TO` = userId หรือ groupId ปลายทาง
ไม่ได้ตั้งค่า = ไม่ส่งอะไรเลย ระบบเดิมทำงานปกติ (`pushLine_` เช็คก่อนยิง)

ทั้งก้อนครอบด้วย try/catch — LINE ล่มหรือ token หมดอายุ การบันทึกผลอนุมัติยังทำงานปกติ

**แจ้งเตือน 3 เหตุการณ์**
- 🔔 ผู้อนุมัติกด "มีคำถาม" — ดูจาก `status` = `question|SLOT` (2 ส่วน)
- ✅ A1 หรือ A2 กด Approve — ถ้าครบ 2 คนขึ้น 🎉 พร้อมลิงก์ไปปิดการขาย
- ❌ กด Reject — พร้อมหมายเหตุว่าทำไม

**ห้ามดูแค่ `status` ตอนแจ้งเตือนการโหวต** — `undoClose()`, `cancelExtRequest()`,
`restoreOffer()`, `saveApprovedPrice()` ก็ยิง `approved`/`rejected` เหมือนกัน ทั้งที่ไม่ใช่การเคาะใหม่
`vote()` จึงส่ง `event:'vote'` + `voteSlot` + `voteDecision` มาด้วย และฝั่ง Apps Script
เชื่อเฉพาะ `event === 'vote'` เท่านั้น

**รับ webhook จาก LINE ที่ URL เดียวกับ doPost** — `handleLineWebhook_()` ดักก่อน
(payload ของ LINE มี `events[]`) เก็บ groupId เฉพาะตอน `LINE_CAPTURE = '1'` และเฉพาะข้อความจากกลุ่ม
เก็บเสร็จปิดสวิตช์เอง คนนอกทักบอทจึงแย่งเปลี่ยนปลายทางไม่ได้
ฟังก์ชัน `showNotifyTarget()` กด Run เพื่อดูว่าตอนนี้ยิงไปที่ไหน

**แยกปลายทางตามเหตุการณ์** (Version 15 ขึ้นไป) — **ตอนนี้ไม่ได้ใช้**
กลไกยังอยู่ในโค้ด แต่**ตอนนี้ปิดแจ้งเตือนทั้งหมดแล้ว** ดูหัวข้อ "แจ้งเตือน LINE: ปิดไว้ทั้งหมด"
- `LINE_TO` = กลุ่มหลัก รับ คำถาม / อนุมัติคนแรก / ปฏิเสธ
- `LINE_TO_APPROVED` = กลุ่มที่รับเฉพาะ **"อนุมัติครบ 2 คน"**
- ไม่ได้ตั้ง `LINE_TO_APPROVED` หรือค่าว่าง → ถอยไปใช้ `LINE_TO` อัตโนมัติ
- `pushLine_(text, toKey)` รับชื่อ property ปลายทาง ไม่ระบุ = กลุ่มหลัก

**โหมดเก็บ groupId ระบุปลายทางได้** — `LINE_CAPTURE` เก็บ**ชื่อ property** ที่จะเขียนลงไป
(`LINE_TO` / `LINE_TO_APPROVED` ส่วน `'1'` = รูปแบบเดิม เท่ากับ `LINE_TO`)
เก็บสำเร็จแล้วตั้งกลับเป็น `'0'` เอง

ตอนเก็บ `LINE_TO_APPROVED` จะ**ข้าม groupId ที่ตรงกับ `LINE_TO`** เพราะเคยพลาดมาแล้ว —
มีคนพิมพ์ในกลุ่มเดิมระหว่างเปิดโหมดเก็บ ระบบเลยคว้ากลุ่มเดิมแล้วปิดโหมดทันที
สองช่องชี้กลุ่มเดียวกัน แยกกลุ่มไม่สำเร็จ

**กับดักตอนตั้งค่า (เสียเวลาไปหลายรอบ)**
- Apps Script **จำสิทธิ์ที่เคยให้ไว้** พอเพิ่ม `UrlFetchApp` ทีหลังมันไม่ขอ scope ใหม่
  แก้ด้วยการถอนสิทธิ์ที่ myaccount.google.com/permissions แล้ว Run ใหม่ให้มันขออีกรอบ
  (ประกาศ `oauthScopes` ใน `appsscript.json` อย่างเดียวไม่พอ)
- ช่องเลือกฟังก์ชันในตัวแก้ไขค้างที่ `setupHeaders` ต้องเปลี่ยนเป็น `testNotify` ก่อนกด Run
- ทดสอบด้วย curl ไม่ได้ Apps Script ตอบ 405 เพราะ redirect — ต้องยิงจากเบราว์เซอร์แบบ `mode:'no-cors'`

**5. ตารางค่าเฟรท** อยู่ในตัวแปร `DEFAULT_FREIGHT` (47 port, 12 กลุ่ม)
แก้ใน Setting = เก็บใน localStorage ของเครื่องนั้นเท่านั้น
ถ้าจะอัปเดตให้ทั้งทีม ต้องแก้ `DEFAULT_FREIGHT` ในโค้ดแล้ว deploy

**6. ใบเสนอราคา vs ใบให้ไอเดียราคา** — เลือกที่หัว Step 2 เป็นของ**ทั้งใบ** ไม่ใช่รายสินค้า
เก็บในตัวแปร `offerKind` (`'offer'` | `'idea'`) สลับด้วย `setOfferKind()`

โหมด `idea` ต่างจากปกติ 4 อย่าง:
- ซ่อนช่อง DATE และ VALID (คลาส `.fld-offer-only`) และล้างค่าใน `getProdData()` กันค่าค้างจากตอนสลับโหมด
- `Shipment ที่จะเสนอ` ไม่บังคับกรอก (ข้ามเช็คทั้งใน `goNext(2)` และ `submitForm()`)
- ป้ายชื่อเปลี่ยนด้วย CSS ล้วน — `.lbl-offer` / `.lbl-idea` คู่กัน สลับตามคลาส `.kind-idea` บน `#products-container`
  (การ์ดสินค้าถูกสร้างด้วย JS ทีหลังได้เรื่อย ๆ การใช้ CSS จึงครอบคลุมทั้งการ์ดเดิมและการ์ดใหม่)
- ฝั่งผู้อนุมัติ/เช็คสถานะ ขึ้นป้าย "ไอเดียราคา" ที่หัวการ์ด เปลี่ยนคำ "ราคาเสนอ" เป็น "ไอเดียราคา"
  และซ่อนปุ่ม "ขอขยาย VALID" เพราะใบไอเดียไม่มี VALID ให้ขยาย

โหมดเป็นของทั้งใบ แต่ `savedSessions` ถูกบันทึกไว้ตอนกด "เพิ่มสินค้า" ซึ่งอาจเป็นคนละโหมด
`submitForm()` จึงเขียนโหมดปัจจุบันทับทุก session ก่อนส่ง

**7. ใบเดิมถูกตีเป็น bidding ก็ต่อเมื่อใบใหม่ส่งสำเร็จ** (แก้ 16 ก.ย. 2026)

เดิม `requestBidding()` เขียน `superseded` ลงชีท**ทันทีที่กดปุ่ม** แล้วค่อยเปิดฟอร์มใบใหม่
ถ้าคนกดปิดหน้าไป กดพลาด หรือส่งไม่ผ่าน (เช่นชนเพดาน URL 12,000 ตัว) ใบเดิมจะค้างเป็น
`superseded` ตลอดไปโดยไม่มีใบใหม่มาแทน แล้ว**หลุดจากตะกร้า "รอปิดการขาย"** เพราะ
`getStatus()` คืน `superseded` ก่อนถึงบรรทัดที่เช็ค approved ดีลจึงหายไปจากเรดาร์ทั้งที่อนุมัติครบแล้ว

เกิดขึ้นจริงกับ `SFD-2026-8441` (OKAYA/ASAHI, KAN, 15 ก.ย.) — อนุมัติครบ 2 คน กด bidding
แล้วไม่มีใบใหม่ตามมาเลย ทีมขายรายงานว่า "การ์ดหาย"

ตอนนี้ `requestBidding()` แค่จดคำสั่งไว้ใน `window._supersedeOnSubmit`
แล้ว `applySupersede_()` ที่ถูกเรียกจาก **`showSuccess()` จุดเดียวเท่านั้น** เป็นคนเขียนลงชีท
— `showSuccess()` ขึ้นเฉพาะตอน server ตอบ ok จริง จึงยืนยันได้ว่าใบใหม่ลงชีทแล้วแน่นอน

คำสั่งค้างถูกล้าง 3 ทาง กันไม่ให้ไปติดใบถัดไป: `resetForm()`, `switchRole()`
และ `reviseOffer()` เมื่อเข้ามาด้วยโหมดอื่น (แก้ใบที่ถูกปฏิเสธ)

**7.1 ใบเดิมออกจากแท็บ bidding เมื่อใบใหม่อนุมัติครบ** (17 ก.ย. 2026)

แท็บ bidding แสดงเฉพาะใบเดิมที่**ยังไม่จบเรื่อง** — `biddingDone_()` เป็นจริงเมื่อมีใบใหม่
อยู่ในสถานะ `approved` / `idea-given` / `closed-*` / `not-offered` ใบเดิมจะหายจากแท็บ
(สถานะในชีทยังเป็น `superseded` ไม่ได้เขียนอะไรเพิ่ม เป็นแค่การกรองตอนแสดง)
ใบใหม่ที่ยังรออนุมัติ มีคำถาม ถูกปฏิเสธ หรือถูกลบ ไม่นับ ใบเดิมยังอยู่ให้กด "ยกเลิก bidding" ได้
การ์ดใบเดิมบอกว่าใบใหม่คือ REF ไหน สถานะอะไร

**ราคาตั้งต้นไม่หาย** — การ์ดใบใหม่มีกล่องฟ้า "Bidding จาก SFD-…" บอกราคาที่ใบเดิมเสนอ
ราคาที่อนุมัติ และวันที่ส่ง (`bidHistory_()`) ขึ้นทั้งหน้า Approve และหน้าเช็คสถานะ

**จับคู่ใบใหม่กับใบเดิมยังไง** — `bidParentRef_()` อ่าน meta `BID:` ก่อน
(`submitForm()` เติมให้เมื่อ `window._supersedeOnSubmit` มีค่า ทีมขายแก้ไม่ได้)
ใบที่ส่งก่อน 17 ก.ย. ไม่มี meta นี้ จึงถอยไปหา "ต่อรองจาก SFD-…" ในหมายเหตุ
ซึ่ง `reviseOffer()` เติมให้ แต่**ทีมขายลบทิ้งได้** — เกิดจริงแล้ว: `SFD-2026-5949` (Marination DK)
เป็น bidding ของ `SFD-2026-7869` แต่ KAN พิมพ์หมายเหตุทับ ระบบเลยจับคู่ไม่ได้
และ 7869 จะค้างในแท็บ bidding จนกว่าจะเติม `ต่อรองจาก SFD-2026-7869` ต่อท้ายช่องหมายเหตุของ 5949 ในชีท

**เพิ่มปุ่ม "ยกเลิก bidding"** บนการ์ดในแท็บ bidding ของหน้าเช็คสถานะ (`undoBidding()`)
ใช้กับใบที่ค้างอยู่แล้วได้ — เดิมสถานะ `superseded` **ไม่มี branch แสดงปุ่มอะไรเลย**
การ์ดจึงมองเห็นแต่ทำอะไรไม่ได้ กดแล้วใบกลับไปเป็น `approved` ตามผลโหวตเดิม

---

## ทดสอบในเครื่อง — กับดัก preview server

`preview_start` **ไม่ได้เสิร์ฟโฟลเดอร์โปรเจกต์** แต่เสิร์ฟ `scratchpad/serve/` ซึ่งเป็น
**สำเนา** ของ `index.html` และสำเนานั้นค้างอยู่ข้ามเซสชัน แก้โค้ดแล้วรีเฟรชจะยังเห็นของเก่า
โดยไม่มีอะไรเตือน (เสียเวลาไปรอบหนึ่งแล้ว 16 ก.ย. — นึกว่าโค้ดที่เพิ่งเขียนไม่ทำงาน)

ทำ symlink แทนไม่ได้ python `http.server` ตอบ 404 — ต้อง **copy ทับก่อนทดสอบทุกครั้ง**
```bash
cp index.html "$(lsof -a -p $(pgrep -f 'http.server' | tail -1) -d cwd | tail -1 | awk '{print $NF}')/index.html"
```
เช็คว่าตรงกันด้วย `md5 -q` ทั้งสองไฟล์ก่อนเชื่อผลทดสอบ
เปิดไฟล์ตรง ๆ ด้วยเบราว์เซอร์ก็ได้เหมือนกัน ไม่ต้องมี server

---

## วิธี deploy

**ทำเมื่อผู้ใช้สั่งเท่านั้น และต้องขึ้นทั้งสองที่**

**ขั้นตอนต่างกันตามเครื่อง — เช็คก่อนว่ามี `gh` / `node` ไหม**

**GitHub** — repo คือ `nichasenapeng/Offer_price`

เครื่องที่มี `gh` และ active เป็น `aiengsunfood` (push ไม่ได้) ต้องสลับก่อน:
```bash
gh auth switch -u nichasenapeng
git -c credential.helper='!gh auth git-credential' push origin main
gh auth switch -u aiengsunfood
```
เครื่องที่ไม่มี `gh` แต่มี credential ของ nichasenapeng ใน macOS keychain
(`credential.helper = osxkeychain`) push ตรงได้เลย ไม่ต้องสลับอะไร:
```bash
git push origin main
```

**Cloudflare** — worker `sunfood-offer` อยู่บนบัญชีของ nicha (`186b05df927d0192ed92ec799dc1b2a0`)
copy `index.html` ไปเป็น `public/index.html` คู่กับ `wrangler.toml`:
```toml
name = "sunfood-offer"
compatibility_date = "2026-01-01"
[assets]
directory = "./public"
```
```bash
CLOUDFLARE_ACCOUNT_ID=186b05df927d0192ed92ec799dc1b2a0 npx wrangler deploy
```

**เช็ค `wrangler.toml` ทุกครั้งก่อนกด deploy** — โฟลเดอร์ deploy อยู่ใน `/tmp` ซึ่งระบบล้างทิ้งได้
ถ้า `wrangler.toml` หาย wrangler จะ**สร้าง config ใหม่ให้เองโดยตั้งชื่อ worker ตามชื่อโฟลเดอร์**
แล้วไปสร้าง worker ตัวใหม่แทนที่จะอัปเดต `sunfood-offer` (เคยเกิดจริง 15 ก.ย. 2026
ได้ worker เกินมาชื่อ `cf-deploy` พร้อม URL สาธารณะ ต้องลบทิ้งด้วย
`npx wrangler delete --name cf-deploy`)

ดูบรรทัดผลลัพธ์ให้ขึ้นว่า `Uploaded sunfood-offer` ถ้าขึ้นชื่ออื่น = ผิดตัว
token หมดอายุบ่อย และเวลา refresh เองมันเด้งกลับไปเป็นบัญชี ai.eng.sunfood ซึ่งเข้า worker ของ nicha ไม่ได้
ต้อง `npx wrangler login` ใหม่ แล้วให้ผู้ใช้กด Allow **ภายใน 2 นาที** โดยเบราว์เซอร์ต้อง sign in เป็น nicha อยู่ก่อน
เช็คว่าได้บัญชีถูกด้วย `npx wrangler whoami` — ต้องขึ้น account ID `186b05df...`

**อย่ารัน `wrangler login` โดยไพป์ผ่าน `tail` หรือ `head`** — มันจะ buffer จนจบ process
ทำให้ลิงก์ authorize ไม่โผล่ระหว่างที่ยังกดทัน

เครื่องที่ไม่มี Node เลย (ไม่มี `node` / `npx` / Homebrew) ต้องให้ผู้ใช้ลง Node LTS
จาก nodejs.org ก่อน เพราะขั้นตอนนี้ต้องใช้รหัสเครื่อง ทำแทนไม่ได้
บนเครื่องแบบนั้น token จะไปอยู่ที่ `~/Library/Preferences/.wrangler/config/default.toml`

เสร็จแล้วเทียบ md5 ของทั้งสอง URL กับไฟล์ในเครื่องก่อนบอกว่าเสร็จ

---

## ผู้ใช้

**Export Sales Team** — NICHA · NET · KAN · EMMA · MAI
**Food Supply Chain** — MAY · MILK · ANCHAN · TIK · ICE
**ผู้อนุมัติ** — Approver 1 (A1), Approver 2 (A2) · รหัสผ่าน `approvexx` (ตัวแปร `PASS` ในโค้ด)
เปลี่ยนรหัส = แก้ `PASS` แล้ว deploy — โหมด Status และ Summary ไม่ต้องใช้รหัส เข้าได้เลยเหมือนเดิม

---

## แจ้งเตือน LINE: ปิดไว้ทั้งหมด (21 ก.ย. 2026)

**ตอนนี้ไม่ส่งแจ้งเตือนเข้ากลุ่มไหนเลย** — ตั้งใจปิด ไม่ใช่ระบบเสีย

ปิดด้วยการ**ลบค่าใน Script Properties** ของ Script 2 (`LINE_TO`, `LINE_TO_APPROVED`
และตั้ง `LINE_CAPTURE` = `0`) ไม่ได้แก้โค้ดและไม่ได้ deploy อะไร เพราะ `pushLine_`
เช็คค่าปลายทางก่อนยิงอยู่แล้ว ไม่มีค่า = ไม่ส่ง ระบบอนุมัติทำงานปกติทุกอย่าง

**ที่มา** — แผนเดิมคือย้ายจากกลุ่ม `Export sales team` ไป `Dekexport 2025` แต่ติดที่
**บอทเข้ากลุ่มใหม่แล้วออกเองทุกครั้ง** (ลองซ้ำถึง 16 ก.ย. ก็ยังเหมือนเดิม) แล้วต่อมา
**แจ้งเตือนในกลุ่มเดิมก็เงียบไปด้วย** ยังไม่ได้หาสาเหตุ — ที่สงสัยไว้คือ token หมดอายุ
โควตาข้อความฟรีของเดือนหมด หรือ `LINE_TO` ชี้ผิดกลุ่ม ดูได้จากเมนู "การดำเนินการ"
ของ Apps Script หลังกด Run `testNotify` (401 = token, 429 = โควตา, 400 = ปลายทางผิด)
สุดท้ายตัดสินใจปิดไปก่อน ไม่ไล่ต่อ

**เปิดกลับยังไง** — ใส่ `LINE_TOKEN` กับ `LINE_TO` กลับเข้า Script Properties
ทำงานต่อได้ทันที โค้ดแจ้งเตือนทั้งชุด (3 เหตุการณ์ + webhook + แยกปลายทาง) ยังอยู่ครบ ไม่ได้รื้อ

**สมมติฐานที่ยังไม่ได้ตรวจ ถ้าวันหนึ่งกลับมาทำเรื่องบอทเข้ากลุ่ม**
หลักฐานชี้ว่าน่าจะเป็น**โค้ดสั่ง leave เอง** ไม่ใช่การตั้งค่า LINE OA:
webhook ได้รับ event `join` จนเก็บ groupId ได้ (ถ้าสิทธิ์ปิด LINE จะเตะออกโดยไม่ส่ง event),
ออกหลังเข้า 2 วินาที (LINE เตะออกเองจะทันที), และในกลุ่มขึ้นว่า "ออกจากกลุ่ม" ไม่ใช่ "ถูกนำออก"
→ ค้นคำว่า `leave` ใน Script 2 ดูว่ามีการยิงไป `/v2/bot/group/{groupId}/leave` หรือไม่
groupId ของ Dekexport 2025 ที่เคยเก็บได้: `Ca52dcb375c6da830eaa9a4000abfe6d3`

**โค้ดใน editor ที่ยังไม่ได้ deploy** — ตัวตรวจรูปแบบปลายทาง (`/^[CUR][0-9a-f]{32}$/`)
ค้างอยู่ใน editor ของ Script 2 ถูก deploy ติดไปกับ Version 17 แล้ว (23 ก.ย.) ไม่มีผลเพราะปิดแจ้งเตือนอยู่

## ข้อจำกัดที่ยังอยู่

- **รหัสผ่านผู้อนุมัติกันได้แค่คนกดผิดหน้า ไม่ใช่ระบบยืนยันตัวตน** — ไฟล์นี้เป็น HTML ล้วน
  ที่เปิดสาธารณะ ใครกด View Source ก็เห็นค่า `PASS` ตรง ๆ และรหัสเดียวใช้ร่วมกันทั้ง A1 และ A2
  ระบบยังแยกไม่ได้ว่าใครเป็นคนกด ถ้าต้องการของจริงต้องย้ายการตรวจสอบไปฝั่ง Apps Script
- ค่าใน Setting เก็บแยกตามเครื่อง ไม่ sync ข้ามผู้ใช้
- ใบที่ส่งก่อน 10 ก.ย. 2026 และมีหลายสินค้า จะไม่เห็นหมายเหตุ (ข้อมูลค้างอยู่แถวหลัง)
- **REF เคยชนกันจริง** (23 ก.ย. 2026) — ใบ MITSUI ของ EMMA สุ่มได้ `SFD-2026-1612` ซึ่งตรงกับใบ UMIOS ของ MAI
  ที่อนุมัติไปแล้วตั้งแต่ 21 ก.ย. Script 2 จัดกลุ่มตาม REF สินค้าสองชิ้นของ MITSUI จึงไปโผล่ในการ์ด UMIOS
  และดูเหมือนอนุมัติแล้ว ส่วนใบ MITSUI เองไม่เคยขึ้นให้ผู้อนุมัติเห็นเลย
  ตอนนี้ `submitForm()` เรียก `pickUniqueRef_()` ดึงรายการ REF จาก Script 2 มาเช็คก่อน แล้วสุ่มใหม่จนกว่าจะไม่ซ้ำ
  ถ้าดึงไม่ได้ภายใน 8 วินาที จะเช็คกับ `DATA` ที่โหลดไว้ในเครื่องแทน และยังส่งได้ตามปกติ
  ยังชนได้ถ้าสองคนกดส่งพร้อมกันและสุ่มได้เลขเดียวกันพอดี ซึ่งโอกาสน้อยมาก
  ถ้าเจออีก ให้แก้ REF ของแถวใบที่ส่งทีหลังในชีท Offer เป็นเลขที่ยังไม่มีใครใช้
